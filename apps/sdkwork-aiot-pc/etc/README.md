# AIoT PC Source Configuration

This surface delegates deployment and topology ownership to the AIoT application root. The browser
runtime contract lists renderer bindings only; it does not copy parent topology profiles.
