# AIoT Topology Profiles

These tracked profiles are selected by `specs/topology.spec.json`. Development profiles contain
non-secret local or deployed URLs. Production profiles contain only safe values and explicit
deployment-time placeholders for secrets and infrastructure coordinates.
